define(['jquery', 'bootstrap', 'backend', 'table', 'form'], function ($, undefined, Backend, Table, Form) {

    var Controller = {
        index: function () {
            // 客服功能核心代码已经直接写在 application/admin/view/lottery/kefu/index.html 中
            // 这里返回基本的 Controller 防 404 错误即可。
        }
    };
    return Controller;
});
